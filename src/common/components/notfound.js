/* @flow */
import React from 'react';
import { Route } from 'react-router';

type Props = {};

export default function NotFound(props: Props) {
    return (
        <div>
            File not found
        </div>
    );
}
